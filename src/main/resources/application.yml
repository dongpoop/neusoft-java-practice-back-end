server:
  port: 8888

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/shop?serverTimeZone=GMT+8&....

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT + 8
logging:
  level:
    com.neuedu.mapper: debug

config:
  pageSize: 6